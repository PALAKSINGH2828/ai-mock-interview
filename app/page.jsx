import { Button } from "@/components/ui/button";
import Image from "next/image";

export default function Home() {
  return (
   <div>
    <h1>WRITE</h1>
    <Button>/dashboard (on above URL)</Button>
   </div>
  );
}